'use strict';
const titleElement = document.querySelector('.title');
const listElement = document.querySelector('.item-uno');

titleElement.innerHTML = titleElement.innerHTML + ' ' + listElement.innerHTML;